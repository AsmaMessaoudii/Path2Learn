<header class="navbar navbar-expand-xl bg-white border-bottom px-4 sticky-top"
        style="height: 70px;">

    <div class="container-fluid p-0">

        <!-- Search -->
        <div class="d-flex align-items-center flex-grow-1">
            <form class="position-relative w-100" style="max-width: 450px;">
                <span class="position-absolute top-50 start-0 translate-middle-y ms-3">
                    <i class="bi bi-search fs-5 text-dark"></i>
                </span>

                <input class="form-control bg-light border-0 ps-5 py-2"
                       type="search"
                       placeholder="Rechercher un cours, un projet..."
                       style="border-radius: 12px;">
            </form>
        </div>

        <!-- Right -->
        <ul class="navbar-nav ms-auto align-items-center">

            <!-- Notification -->
            <li class="nav-item me-3">
                <a class="nav-link position-relative bg-light rounded-circle d-flex align-items-center justify-content-center"
                   href="#"
                   style="width: 40px; height: 40px;">
                    <i class="bi bi-bell-fill"></i>

                    <span class="position-absolute top-0 start-100 translate-middle bg-danger rounded-circle"
                          style="width:10px; height:10px;"></span>
                </a>
            </li>

            <!-- Profile -->
            <li class="nav-item dropdown">
                <a class="nav-link p-0 d-flex align-items-center"
                   href="#"
                   id="profileDropdown"
                   data-bs-toggle="dropdown">

                    <div class="me-2 d-none d-sm-block text-end">
                        <div class="fw-bold text-dark" style="font-size: 14px; line-height: 1.1;">
                            Admin
                        </div>
                        <div class="text-muted" style="font-size: 11px; line-height: 1.1;">
                            Administrateur
                        </div>
                    </div>

                    <div class="avatar">
                        <img class="avatar-img rounded-circle border border-2 border-primary"
                             src="{{ asset('assets/images/avatar/02.jpg') }}"
                             alt="user"
                             style="width: 40px; height: 40px;">
                    </div>
                </a>

                <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2">
                    <li><a class="dropdown-item" href="{{ path('admin_account_profile') }}"><i class="bi bi-person me-2"></i> Profil</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_account_security') }}"><i class="bi bi-lock me-2"></i> Sécurité</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item text-danger" href="#"><i class="bi bi-box-arrow-right me-2"></i> Déconnexion</a></li>
                </ul>
            </li>

        </ul>
    </div>
</header>
